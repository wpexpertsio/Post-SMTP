{
    "name": "post-smtp/post-smtp",
    "description": "Post SMTP WordPress plugin — greenfield revamp",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "autoload": {
        "psr-4": {
            "PostSMTP\\": "src/",
            "PostSMTP\\Kernel\\": "kernel/"
        }
    },
    "require": {
        "php": ">=7.4"
    },
    "config": {
        "sort-packages": true
    }
}
